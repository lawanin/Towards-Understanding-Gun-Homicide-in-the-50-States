---
title: "test"
author: "Nosa Lawani"
date: "3/24/2021"
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
library(Tidyver)
```

```{r}
crime_gdp %>%
  ggplot(aes(x = gdp_id, y = crime_per_capita)) +
  geom_point() + 
  labs(title = "Violent crime by states in Order of asecending GDP per capita", 
       subtitle = "More Poor States seem to have higher violence", 
       x = "States in order of ascending GDP per capita", 
       y = "Crime per capita") +
    scale_x_continuous(breaks = c(1 : 50)) +
    geom_smooth() 
  
```

