---
title: "test"
author: "Nosa Lawani"
date: "3/24/2021"
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
library(tidyverse)
library(rstanarm)
```

```{r}
a <- stan_glm(data = crimegdpfile, 
                  formula = big_crime_per_capita ~ gdp_id, 
                  refresh = 0, 
                  seed = 87)

print(a, digits = 4)
```
```{r}
crime_gdp %>% 
  ggplot(aes(x = gdp_id, y = big_crime_per_capita)) +
  geom_point(alpha = .5) +
  geom_smooth(method = "lm") 
```

